<h1> <%= @user.username %>'s Plant Journal </h1>

<% if @needs_water != [] %>

<h4> Your Plants Needing to be Watered </h4>
 <% @needs_water.each do |p| %>
    <%= link_to p.plant.name, user_journal_path(@user.id, p) %>: Last watered on <%=p.last_watered %></br>
    <%end%>

    <%end%> </br>
    </br>


<% if @user.journals %>
<h4>Journal Entries:</h4>
<% @user.journals.each do |journal| %>
    <div><%= link_to journal.plant.name, user_journal_path(journal.user, journal)%></div> 
<% end %>
<% end %>
</br>
</br>

<%= link_to "New Entry", new_user_journal_path(@user) %>

